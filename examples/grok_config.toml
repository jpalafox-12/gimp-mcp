# Paste into ~/.grok/config.toml or project .grok/config.toml
# Requires: pip install "git+https://github.com/mergeos-bounties/gimp-mcp.git"

[mcp_servers.gimp_mcp]
command = "gimp-mcp"
args = ["serve"]
env = { GIMP_MCP_MODE = "mock" }
enabled = true
